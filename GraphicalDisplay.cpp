// GraphicalDisplay.cpp
//
// Created by Simran Thind, Janakitti Ratana-Rueangsri, Zuomiao Hu
// on 2021-07-16.


#include "GraphicalDisplay.h"
#include "GenericBlock.h"
#include <memory>
#include <iostream>
#include <vector>

// TODO:
GraphicalDisplay::GraphicalDisplay() {}
GraphicalDisplay::~GraphicalDisplay() {}
void GraphicalDisplay::update() {
    // std::cout << "UPDATE" << std::endl;
    // std::vector <std::vector <std::pair<BlockType, int>>> grid = b->getGrid();
    // std::shared_ptr<GenericBlock> curBlock = b->getCurBlock();
    // int r = curBlock->getRotation();
    // std::pair <int, int> base = curBlock->getCoords();
    // std::pair <int, int> a = curBlock->getCells().at(r).at(0);
    // std::pair <int, int> b = curBlock->getCells().at(r).at(1);
    // std::pair <int, int> c = curBlock->getCells().at(r).at(2);
    // std::pair <int, int> d = curBlock->getCells().at(r).at(3);
    // for(std::size_t i = 0; i < grid.size(); ++i) {
    //     for(std::size_t j = 0; j < grid.size(); ++j) {
    //         if (j == base.first+a.first && i == base.second+a.second ||
    //             j == base.first+b.first && i == base.second+b.second ||
    //             j == base.first+c.first && i == base.second+c.second ||
    //             j == base.first+d.first && i == base.second+d.second ||) {
    //                 std::cout << "X";
    //         } else {
    //             std::cout << b->getCell(j, i).first;
    //         }
    //     }
    //     std::cout << std::endl;
    // }
}